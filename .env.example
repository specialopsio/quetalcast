# ──────────────────────────────────────────────
# Server
# ──────────────────────────────────────────────

# Port the server listens on (default: 3001)
PORT=3001

# Allowed CORS origin (default: * — set to your domain in production)
ALLOWED_ORIGIN=*

# Require TLS for cookies (set to "true" in production behind HTTPS)
REQUIRE_TLS=false

# Secret for signing session cookies (change this!)
SESSION_SECRET=change-me-to-a-random-string

# Password for broadcaster login
ADMIN_PASSWORD=change-me

# ──────────────────────────────────────────────
# TURN relay (pick ONE method)
# ──────────────────────────────────────────────

# Option A — Metered.ca dynamic credentials (recommended)
METERED_APP_NAME=
METERED_API_KEY=

# Option B — Static TURN server
TURN_URL=
TURN_USERNAME=
TURN_CREDENTIAL=

# ──────────────────────────────────────────────
# Audio Identification (AcoustID)
# ──────────────────────────────────────────────

# AcoustID API key for automatic song identification
# Get one free at: https://acoustid.org/new-application
# Also requires fpcalc (Chromaprint) installed on the server
ACOUSTID_API_KEY=

# ──────────────────────────────────────────────
# Logging
# ──────────────────────────────────────────────

# Directory for server log files (default: server/logs)
LOG_DIR=

# Log level: error, warn, info, debug (default: info)
LOG_LEVEL=info

# ──────────────────────────────────────────────
# Frontend (Vite)
# ──────────────────────────────────────────────

# WebSocket URL override (auto-detected from window.location if omitted)
VITE_WS_URL=

# Enable verbose debug logging in browser console (true / false)
VITE_DEBUG=true
